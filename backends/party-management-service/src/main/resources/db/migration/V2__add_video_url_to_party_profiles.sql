-- Presentation video (YouTube link or direct video URL) alongside the party's other self-edited
-- "about" content (history/program/statute/cover).
ALTER TABLE party_profiles ADD COLUMN video_url text;
